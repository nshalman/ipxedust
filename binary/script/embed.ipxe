#!ipxe
  
set user-class Tinkerbell
echo Welcome to Neverland!

:boot
echo DEBUG: about to autoboot
autoboot || goto shell

:shell
echo DEBUG: about to prompt
# If we get here we failed to boot. Provide a 5 second opportunity to drop to a shell
prompt --key 0x02 --timeout 5000 Press Ctrl-B for the iPXE command line... && shell
